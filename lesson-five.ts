// circle: Area = 𝜋 × radius 2 Area=π×radius 2
function calculateCircleArea(radius: number): number {
    return Math.PI * Math.pow(radius, 2);
}
  
  console.log(calculateCircleArea(5)); // Should print: 78.53981633974483 (approximately)
  